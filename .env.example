# Copy per-app examples instead:
#   apps/web/.env.example     -> apps/web/.env.local
#   apps/mobile/.env.example  -> apps/mobile/.env
#
# Only the Supabase URL and the *anon/publishable* key are ever used by client apps.
# The service-role key must NEVER be placed in any client app env file.
