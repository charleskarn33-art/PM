{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "lib": ["ES2022", "DOM"],
    "noEmit": true,
    // API tests import the web app's query modules (mirrors the vitest alias).
    "paths": {
      "@/*": ["../../apps/web/src/*"]
    }
  },
  "include": ["src/**/*.ts", "perf/**/*.ts", "vitest.config.ts", "vitest.perf.config.ts"]
}
